# Neon PostgreSQL (copy from Neon dashboard → Connection string)
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/stayease?sslmode=require"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Stripe (from stripe.com/dashboard)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Frontend URL (Vercel deployment URL or localhost in dev)
FRONTEND_URL="http://localhost:3000"

# Server port (Railway sets this automatically)
PORT=4000
